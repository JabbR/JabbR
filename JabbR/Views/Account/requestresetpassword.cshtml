@using JabbR;
@{
    Layout = "_layout.cshtml";
}
<div class="user-request-reset-password">
    <form class="form-horizontal" action="@Url.Content("~/account/requestresetpassword")" method="post">
        @Html.ValidationSummary()
        <div class="control-group">
            <div class="controls">
                <div class="input-prepend">
                    <span class="add-on"><i class="icon-user"></i></span>
                    @Html.TextBox("username", "input-block-level", @LanguageResources.Account_UserName)
                </div>
            </div>
        </div> 
        <div class="control-group">
            <div class="controls">
                <button type="submit" class="btn">@LanguageResources.Account_ResetRequest</button>
                <a href="@Url.Content("~/")" class="btn">@LanguageResources.Account_Cancel</a>
            </div>
        </div>
        @Html.AntiForgeryToken()
    </form>
</div>
