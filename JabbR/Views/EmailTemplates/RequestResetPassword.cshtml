@*
    This is the base to 'RequestResetPassword.text' template with From, To and Subject properties.
    This properties will affect the EmailTemplate instance creation on the EmailTemplateEngine.
    Here is the most appropriate place to change the Subject and other properties of the EmailTemplate.
*@
@{
    From = Model.From;
    To.Add(Model.To);
    Subject = "[Jabbr] Password reset";
}